#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* concat(char *strings[], int count) {

	char* str = NULL; // pointer to joined strings
	size_t totalLength = 0; // total length of joined strings
	size_t length = 0; // length of a string
	int i = 0;

	for (i=0; i<count; i++)
}


int main() {
	

	return 0;
}
